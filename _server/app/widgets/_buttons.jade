include ../setup/tile/tile

.buttons.group
  style
    .buttons ul {
      list-style: none;
      padding: 0;
    }

  ul
    each button, name in buttons
      li
        a(class=button, href="#") #{name}